#pragma once

#include <QtWidgets/QMainWindow>
#include <qlabel.h>
#include "scanner.h"

class MainWindow : public QMainWindow
{
	Q_OBJECT

public:
	MainWindow(QWidget *parent = Q_NULLPTR);

private:
	
};
